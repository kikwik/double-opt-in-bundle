{% trans with { '{{ confirm_url }}': confirm_url } from 'KikwikDoubleOptInBundle' %}double_opt_in.email.content{% endtrans %}

{#
here you can use the object variable like this:

    {{ object.email }}

and also generate absolute urls like this (the last 0 parameter is the value for UrlGeneratorInterface::ABSOLUTE_URL):

    {{ router.generate('app_resend_email',{id:object.id},0) }}

#}
